import { CommentconfirmPipe } from './commentconfirm.pipe';

describe('CommentconfirmPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentconfirmPipe();
    expect(pipe).toBeTruthy();
  });
});
