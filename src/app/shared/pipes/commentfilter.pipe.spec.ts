import { CommentfilterPipe } from './commentfilter.pipe';

describe('CommentfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
