import { GeneralHeaderPipe } from './general-header.pipe';

describe('GeneralHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
